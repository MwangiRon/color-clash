{
  "name": "mobile-client",
  "version": "1.0.0",
  "description": "Mobile client for Color Clash game",
  "main": "index.js",
  "scripts": {
    "sync": "npx cap sync",
    "copy": "npx cap copy",
    "open:android": "npx cap open android",
    "open:ios": "npx cap open ios",
    "build:android": "npx cap sync android && npx cap open android",
    "build:ios": "npx cap sync ios && npx cap open ios"
  },
  "keywords": ["mobile", "capacitor", "game"],
  "author": "Ron Macharia",
  "license": "MIT",
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  }
}