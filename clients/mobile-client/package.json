{
  "name": "color-clash-mobile-client",
  "version": "1.0.0",
  "description": "Color Clash Game Mobile Client",
  "main": "index.js",
  "scripts": {
    "start": "npx http-server www -p 8080 -o",
    "dev": "npx http-server www -p 8080",
    "sync": "npx cap sync",
    "copy": "npx cap copy",
    "open:android": "npx cap open android",
    "open:ios": "npx cap open ios",
    "build:android": "npx cap sync android && npx cap open android",
    "build:ios": "npx cap sync ios && npx cap open ios"
  },
  "keywords": [
    "mobile",
    "capacitor",
    "game"
  ],
  "author": "Ron Macharia",
  "license": "MIT",
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  }
}
